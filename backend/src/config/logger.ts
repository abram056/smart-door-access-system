/**
 * logger.ts provides logger configuration for the server.
 */
export const logInfo = (message: string) => {
    console.log(message)
}

export const logError = (message: string) => {
    console.error(message)
}
