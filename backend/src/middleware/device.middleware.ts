/**
 * device.middleware.ts provides device-specific request handling.
 */
export const deviceMiddleware = async (req: unknown, res: unknown, next: () => void) => {
    // TODO: implement device middleware
    next()
}
