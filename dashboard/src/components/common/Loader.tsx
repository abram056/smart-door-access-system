import './Loader.css'

/**
 * Loader component displays a loading indicator for async states.
 */
const Loader = () => {
    return (
        <div className="loader">
            <span>Loading...</span>
        </div>
    )
}

export default Loader
